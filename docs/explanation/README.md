# Explanation

Understanding-oriented material: why the tool works the way it does. This
is where design decisions, methodology and architectural reasoning live,
as opposed to step-by-step instructions (`../tutorials/`, `../how-to/`) or
generated technical reference (`../reference/`).

Contents:
- `adr/` — architecture decision records, numbered and dated on the day
  each decision was actually made. Start here as a technical reviewer:
  this is the second thing to open after the README, and the way design
  boundaries are recorded as decisions with reasons instead of a
  known-limitations list.
- `METHODOLOGY.md` — accretes per build stage; every metric shown to a
  user is defined here once. Not yet written (starts in Stage 1, once the
  engine exists).
- `VALIDATION.md` — generated by `daysofcover validate --report` from the
  test suite's own results, so its numbers cannot drift from what the
  tests actually assert. Not yet generated (the validation tier is built
  in Stage 1).
- `ARCHITECTURE.md` — a diagram-and-prose overview of how the engine, API
  and front end fit together. Not yet written.

## Architecture decision records

| ADR | Decision |
| --- | --- |
| [001](adr/001-shared-shock-hazard-groups.md) | Shared-shock hazard groups over independent node failures |
| [002](adr/002-highs-via-scipy.md) | HiGHS via scipy.optimize over OR-Tools |
| [003](adr/003-stateless-api.md) | Stateless API, client-held results |
| [004](adr/004-docker-image-ships-ui-first.md) | Docker image ships UI and precomputed results from Stage 0; wheel bundling deferred to v1.0 |
| [005](adr/005-recovery-as-three-point-input.md) | Recovery as a three-point input with ramp recovery, never an output |
| [006](adr/006-and-or-screen-over-centrality.md) | AND/OR flow-weighted screen over centrality |
| [007](adr/007-svelte-flow-over-cytoscape.md) | Svelte Flow over Cytoscape.js |
| [008](adr/008-own-engine-over-simpy.md) | Own daily-step engine over SimPy, with the spike's numbers |

Later ADRs (enumerated worst case after Stage 4; v1.0 scope boundaries) are
added as those decisions are actually made, not written in advance.
