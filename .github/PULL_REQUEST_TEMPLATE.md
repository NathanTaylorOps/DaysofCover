## What this changes

One paragraph. Which issue or milestone it belongs to.

## How it was checked

- [ ] `uv run ruff check .` and `uv run ruff format --check .` clean
- [ ] `uv run mypy` clean
- [ ] `uv run pytest` passes, and new behaviour has a test
- [ ] Docs touched in the same change where behaviour changed
- [ ] No real company data, names or credentials anywhere in the diff

## Review notes

Anything a reviewer should look at first, or a decision that could have gone another way.
