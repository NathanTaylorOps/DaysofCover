# Normalise line endings: LF in the repo and in the working copy on every OS.
* text=auto eol=lf

# Windows-only files keep CRLF.
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# Binary assets.
*.png binary
*.jpg binary
*.woff2 binary
*.ico binary
